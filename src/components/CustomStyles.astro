---
// Importar Google Fonts: Montserrat (para headings) e Inter (para body)
---

<style is:inline>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Montserrat:wght@600;700&display=swap');

  :root {
    --aw-font-sans: 'Inter', sans-serif;
    --aw-font-serif: 'Inter', serif;
    --aw-font-heading: 'Montserrat', sans-serif;

    /* Colores corporativos ASL */
    --aw-color-primary: rgb(0 51 102); /* Azul marino #003366 */
    --aw-color-secondary: rgb(239 68 68); /* Rojo vibrante #EF4444 */
    --aw-color-accent: rgb(229 231 235); /* Gris claro #E5E7EB */

    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: rgb(255 255 255);

    --aw-color-bg-page-dark: rgb(3 6 32);

    ::selection {
      background-color: rgb(0 51 102 / 20%);
    }
  }

  .dark {
    --aw-font-sans: 'Inter', sans-serif;
    --aw-font-serif: 'Inter', serif;
    --aw-font-heading: 'Montserrat', sans-serif;

    /* Colores corporativos ASL en modo oscuro */
    --aw-color-primary: rgb(30 144 255); /* Azul más claro para dark mode */
    --aw-color-secondary: rgb(239 68 68); /* Rojo vibrante */
    --aw-color-accent: rgb(55 65 81); /* Gris oscuro */

    --aw-color-text-heading: rgb(247, 248, 248);
    --aw-color-text-default: rgb(229 236 246);
    --aw-color-text-muted: rgb(229 236 246 / 66%);
    --aw-color-bg-page: rgb(3 6 32);

    ::selection {
      background-color: black;
      color: snow;
    }
  }
</style>
